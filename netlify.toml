# If the build ever fails on missing semver files, trigger "Clear cache and deploy" once in Netlify.
[build]
  command = "npm run build"
  publish = "public"

[build.environment]
  NODE_VERSION = "18.20.4"
  NPM_FLAGS = "--legacy-peer-deps"
  NODE_OPTIONS = "--openssl-legacy-provider"
